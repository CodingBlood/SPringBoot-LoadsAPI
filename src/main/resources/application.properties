# changing the server port
server.port=8080

# note======================================
# Expose this port via command (ngrok http 9090)
# sudo  docker exec -it mysql1 mysql -uroot -p        // command to mysql database of docker
# sudo docker ps    // details of all the instances available in docker
# note======================================

#database configuration:postgresql
spring.datasource.url=jdbc:postgresql://localhost:5432/load
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=K@rtik2002

#hibernate configuration
#For PostgresSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
